// examples/demo.ltl — runnable demo for ltl_prom_expose

import std::io
use ltl_prom_expose as lib

fn main() {
    io::println("ltl_prom_expose demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}